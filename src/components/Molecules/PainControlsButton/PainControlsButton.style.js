import { StyleSheet } from 'react-native';

const styles = new StyleSheet.create({
  button: {
    flexGrow: 1,
    padding: 5,
    alignItems: 'center',
    margin: 10,
  },
});

export default styles;
